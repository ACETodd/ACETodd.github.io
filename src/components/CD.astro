<div class="cd">
  <div class="cd-ring outer"></div>
  <div class="cd-ring hole"></div>
  <div class="cd-ring dotted"></div>
  <div class="cd-ring inner"></div>
  <div class="cd-ring middle"></div>
  <div class="cd-ring small"></div>
  <div class="cd-ring smaller"></div>
  <div class="cd-ring center"></div>
</div>

<style>
.cd {
  --size: 200px;

  position: relative;
  width: var(--size);
  height: var(--size);
  border-radius: 50%;

  background:
    radial-gradient(circle at 50% 50%,
      #ffffff 0%,
      #d1d1d1 25%,
      #a8a8a8 50%,
      #808080 75%,
      #cfcfcf 100%
    ),
    conic-gradient(
      from 0deg,
      rgba(255, 0, 255, 0.3) 0deg,
      rgba(0, 255, 255, 0.3) 60deg,
      rgba(255, 255, 0, 0.3) 120deg,
      rgba(255, 0, 255, 0.3) 180deg,
      rgba(0, 255, 255, 0.3) 240deg,
      rgba(255, 255, 0, 0.3) 300deg,
      rgba(255, 0, 255, 0.3) 360deg
    );

  background-blend-mode: screen, overlay;
}

.cd-ring {
  position: absolute;
  left: 50%;
  top: 50%;
  border-radius: 50%;
  transform: translate(-50%, -50%);
}

.outer {
  width: 96%;
  height: 96%;
  border: 0.1px solid white;
  opacity: 0.35;
}

.hole {
  width: 30%;
  height: 30%;
  border: 0.75px solid white;
  backdrop-filter: blur(4px);
}

.dotted {
  width: 28.6%;
  height: 28.6%;
  border: 5px dotted rgba(229, 231, 235, 0.15);
}

.inner {
  width: 25.4%;
  height: 25.4%;
  border: 0.8px solid white;
  background: #c3c3c5;
  opacity: 0.7;
}

.middle {
  width: 17%;
  height: 17%;
  background: #bdbabc;
}

.small {
  width: 14%;
  height: 14%;
  background: #cfcdcf;
}

.smaller {
  width: 13.4%;
  height: 13.4%;
  background: #e9e4ea;
}

.center {
  width: 12%;
  height: 12%;
  border: 1px solid #c8c7c5;
  background: #f5f5f5;
  box-shadow: inset 0 0 24px -12px rgba(0, 0, 0, 0.3);
}
</style>